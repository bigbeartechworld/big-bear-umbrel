
version: "3.7"
services:
  app:
    image: excalidraw/excalidraw:latest
    restart: unless-stopped
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-excalidraw_app_1
      APP_PORT: "80"
