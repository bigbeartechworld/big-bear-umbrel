version: "3.7"
services:
  big-bear-stalwart-mail:
    image: stalwartlabs/stalwart:v0.14.0
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/data:/opt/stalwart
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-stalwart-mail_big-bear-stalwart-mail_1
      APP_PORT: "8080"
