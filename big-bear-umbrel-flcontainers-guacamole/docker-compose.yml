version: "3.7"
services:
  app:
    image: flcontainers/guacamole:1.6.0
    restart: always
    environment:
      tz: "UTC"
      EXTENSIONS: "auth-totp,auth-ldap"
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-flcontainers-guacamole_app_1
      APP_PORT: "8080"
