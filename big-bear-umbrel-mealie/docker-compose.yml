# Configuration for mealie setup

# Name of the big-bear-mealie application
# Service definitions for the big-bear-mealie application
services:
  # Service name: app
  # The `app` service definition
  app:
    # Name of the container
    container_name: big-bear-mealie
    # Image to be used for the container
    image: hkotel/mealie:v3.3.2
    # Container restart policy
    restart: unless-stopped
    # Volumes to be mounted to the container
    volumes:
      # Mounting the local /app/data directory to ${APP_DATA_DIR}/data inside the container
      - ${APP_DATA_DIR}/data:/app/data
    # Ports mapping between host and container
    ports:
      # Mapping port 7575 of the host to port 9000 of the container
      - "9925:9000"
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-mealie_app_1
      APP_PORT: "9925"
