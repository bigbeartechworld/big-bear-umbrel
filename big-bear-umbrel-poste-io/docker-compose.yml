version: "3.7"
services:
  big-bear-poste-io:
    image: analogic/poste.io:2.5.7
    restart: unless-stopped
    network_mode: host
    environment:
      - TZ=UTC
    hostname: mail.example.com
    tty: true
    volumes:
      - ${APP_DATA_DIR}/data:/data
