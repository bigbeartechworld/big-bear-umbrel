version: "3.7"
services:
  app:
    image: germannewsmaker/myspeed:1.0.9
    volumes:
      - ${APP_DATA_DIR}/data:/myspeed/data
    restart: unless-stopped
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-myspeed_app_1
      APP_PORT: "5216"
