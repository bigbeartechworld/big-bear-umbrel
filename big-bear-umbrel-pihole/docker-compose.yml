
version: "3.7"
services:
  app:
    image: pihole/pihole:2025.08.0
    restart: unless-stopped
    environment:
      TZ: "UTC"
      FTLCONF_webserver_api_password: "your_password"
    volumes:
      - ${APP_DATA_DIR}/etc:/etc/pihole/
      - ${APP_DATA_DIR}/dnsmasq.d:/etc/dnsmasq.d/
    networks:
      - pihole-network
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-pihole_app_1
      APP_PORT: "53"
networks:
  pihole-network:
    driver: bridge
