version: "3.7"
services:
  big-bear-semaphore:
    image: semaphoreui/semaphore:v2.16.34
    restart: unless-stopped
    environment:
      SEMAPHORE_DB_USER: semaphore
      SEMAPHORE_DB_PASS: semaphore
      SEMAPHORE_DB_HOST: big-bear-semaphore-db
      SEMAPHORE_DB_PORT: "3306"
      SEMAPHORE_DB_DIALECT: mysql
      SEMAPHORE_DB: semaphore
      SEMAPHORE_ADMIN_PASSWORD: password
      SEMAPHORE_ADMIN_NAME: admin
      SEMAPHORE_ADMIN_EMAIL: admin@example.com
      SEMAPHORE_ADMIN: admin
      TZ: UTC
    volumes:
      - ${APP_DATA_DIR}/home:/etc/semaphore
      - ${APP_DATA_DIR}/lib:/var/lib/semaphore
    networks:
      - big-bear-semaphore-network
  big-bear-semaphore-db:
    image: mysql:8.0
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/mysql:/var/lib/mysql
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: "yes"
      MYSQL_DATABASE: "semaphore"
      MYSQL_USER: "semaphore"
      MYSQL_PASSWORD: "semaphore"
    networks:
      - big-bear-semaphore-network
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-semaphore_big-bear-semaphore_1
      APP_PORT: "3000"
networks:
  big-bear-semaphore-network:
    driver: bridge
