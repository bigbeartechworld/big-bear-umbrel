version: "3.7"
services:
  app:
    image: dysta/playitgg:1.1.0
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/secrets:/secrets
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-playitgg_app_1
      APP_PORT: "8080"
