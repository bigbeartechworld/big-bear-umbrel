
version: "3.7"
services:
  big-bear-beszel:
    image: henrygd/beszel:0.14.1
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/data:/beszel_data
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-beszel_big-bear-beszel_1
      APP_PORT: "8090"
