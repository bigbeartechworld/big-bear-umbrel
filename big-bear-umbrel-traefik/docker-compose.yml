version: "3.7"
services:
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-traefik_app_1
      APP_PORT: ' 80'
  app:
    image: traefik:v3.7.13
    restart: unless-stopped
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
      TRAEFIK_API: false
      TRAEFIK_API_DASHBOARD: true
      TRAEFIK_API_INSECURE: false
    volumes:
      - type: bind
        source: ${APP_DATA_DIR}/traefik.toml
        target: /etc/traefik/traefik.toml
      - type: bind
        source: ${APP_DATA_DIR}/traefik.yml
        target: /etc/traefik/traefik.yml
      - type: bind
        source: ${APP_DATA_DIR}/config.yml
        target: /etc/traefik/config.yml
      - type: bind
        source: ${APP_DATA_DIR}/acme.json
        target: /etc/traefik/acme.json
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
