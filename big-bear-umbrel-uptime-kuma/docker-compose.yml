
version: "3.7"
services:
  app:
    image: louislam/uptime-kuma:1
    volumes:
      - ${APP_DATA_DIR}/data:/app/data
    restart: always
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-uptime-kuma_app_1
      APP_PORT: "3001"
