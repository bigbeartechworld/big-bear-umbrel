
version: "3.7"
services:
  big-bear-dbgate:
    image: dbgate/dbgate:6.6.5-alpine
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/config:/root/.dbgate
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-dbgate_big-bear-dbgate_1
      APP_PORT: "3000"
