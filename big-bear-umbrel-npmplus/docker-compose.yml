version: "3.7"
services:
  big-bear-npmplus:
    image: zoeyvid/npmplus:449
    restart: unless-stopped
    network_mode: host
    environment:
      - "TZ=America/Chicago"
    volumes:
      - "${APP_DATA_DIR}/data:/data"
