
version: "3.7"
services:
  big-bear-jellystat:
    image: cyfershepard/jellystat:1.1.6
    restart: unless-stopped
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=8a23ad09-61cf-4178-a783-6d4586496e3a
      - POSTGRES_IP=big-bear-jellystat-db
      - POSTGRES_PORT=5432
      - JWT_SECRET="my-secret-jwt-key"
    volumes:
      - ${APP_DATA_DIR}/backup-data:/app/backend/backup-data
    networks:
      - big-bear-jellystat-network
  big-bear-jellystat-db:
    image: postgres:15.2
    environment:
      POSTGRES_DB: "jfstat"
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 8a23ad09-61cf-4178-a783-6d4586496e3a
    volumes:
      - ${APP_DATA_DIR}/postgres-data:/var/lib/postgresql/data # Mounting the volume
    networks:
      - big-bear-jellystat-network
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-jellystat_big-bear-jellystat_1
      APP_PORT: "3000"
networks:
  big-bear-jellystat-network:
    name: big-bear-jellystat-network
    driver: bridge
