
version: "3.7"
services:
  big-bear-budibase:
    image: budibase/budibase:3.22.3
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/data:/data
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-budibase_big-bear-budibase_1
      APP_PORT: "80"
