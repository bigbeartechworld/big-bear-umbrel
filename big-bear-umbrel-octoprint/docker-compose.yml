
version: "3.7"
services:
  app:
    image: octoprint/octoprint:1.11.3
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/octoprint:/octoprint
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-octoprint_app_1
      APP_PORT: "80"
