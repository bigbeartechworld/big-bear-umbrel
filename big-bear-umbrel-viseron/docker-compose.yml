version: "3.7"
services:
  big-bear-viseron:
    image: roflcoopter/viseron:3.3.1
    restart: unless-stopped
    shm_size: "1024mb"
    volumes:
      - ${APP_DATA_DIR}/segments:/segments
      - ${APP_DATA_DIR}/snapshots:/snapshots
      - ${APP_DATA_DIR}/thumbnails:/thumbnails
      - ${APP_DATA_DIR}/event_clips:/event_clips
      - ${APP_DATA_DIR}/config:/config
      - /etc/localtime:/etc/localtime:ro
    environment:
      - PUID=1000
      - PGID=1000
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-viseron_big-bear-viseron_1
      APP_PORT: "8888"
