# Configuration for node-red setup
# Application Name
version: "3.7"
# Services used in the application
services:
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-node-red_app_1
      APP_PORT: "1880"
  # The `app` service for the node-red application
  app:
    # Docker image to be used
    image: nodered/node-red:5.0.2@sha256:c1956020594e674c3c0dd43172b43dd39f9aa743928eb0d076de234cf5d00467
    # Restart policy for the service
    restart: unless-stopped
    # Volumes for the service HOST -> CONTAINER
    volumes:
      - ${APP_DATA_DIR}/data:/data
