version: "3.7"
services:
  app:
    image: nodered/node-red:4.1.1
    network_mode: host
    restart: always
    volumes:
      - type: bind
        source: ${APP_DATA_DIR}/data
        target: /data
