
version: "3.7"
services:
  big-bear-upsnap:
    image: ghcr.io/seriousm4x/upsnap:5.2.3
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/pb_data:/app/pb_data
  app_proxy:
    environment:
      APP_HOST: big-bear-umbrel-upsnap_big-bear-upsnap_1
      APP_PORT: "8090"
